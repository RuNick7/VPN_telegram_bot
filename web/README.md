# Web MVP

This directory contains the initial skeleton for:

- `backend/` - FastAPI backend for the future personal account API
- `frontend/` - static frontend placeholder for landing/cabinet MVP

Details for launching are in the repository root `README.md`.
