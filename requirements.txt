# Upper bounds on everything. `>=` alone meant a build today and a build last
# month could install different majors of aiogram or pydantic, and the first
# anyone would learn of it is a bot that will not start in production. The
# versions pinned here are the ones the suites run against; `~=` allows patch
# and minor updates within a major, which is where the security fixes live.
aiogram~=3.30
aiohttp~=3.11.18
APScheduler~=3.11.3
asyncpg~=0.31.0
httpx~=0.27.2
pydantic~=2.11
pydantic-settings~=2.14
python-dotenv~=1.2
remnawave-api~=1.1.3
yookassa~=3.5.0

# Shared repository layer, Remnawave client, and settings (admin_bot + user_bot)
-e ./shared
